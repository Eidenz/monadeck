# Rust
/target
**/target
Cargo.lock.orig

# Node / SvelteKit
node_modules
/desktop/build
/desktop/.svelte-kit
.DS_Store

# Tauri generated
/desktop/src-tauri/gen

# Staged overlay sidecar (built by scripts/stage-overlay.sh on dev/build)
/desktop/src-tauri/binaries

# Build-time feature config for the overlay (Picsur share, Ollama translate)
/crates/overlay/picsur.env
/crates/overlay/translate.env
