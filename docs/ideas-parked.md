# Parked ideas

Proposals not picked (yet). Revisit after the current batch lands.

(empty — the last batch landed: hidden layout restore, scroll/drag sliders,
controls card, monado-frame retired + wiki update)
