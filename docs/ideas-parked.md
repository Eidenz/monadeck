# Parked ideas

Proposals not picked (yet). Revisit after the current batch lands.

## Screens
- **Window capture** (portal source type Window): parked — unsure it's wanted.
  Input mapping would need a "click to focus" trick since windows report no
  desktop position; would start view-only.
- Per-screen "always on top of games" opacity preset + a quick opacity toggle
  on the watch (dim a HUD screen to ~30 % with one tap).

## Input
- Scroll speed and drag threshold as Behaviour sliders (constants today).
- Middle click / drag-scroll on the thumbstick click (still free).
- Laser-off mode while typing.

## System & comfort
- Session stats on the Monado tab: overlay frame time, PipeWire frames received
  per screen, blit GPU time.
- Auto-pause all captures after a minute of nobody looking while a game is in
  the foreground; toast on resume.
- "Guest" profile for friends: fresh config, no layouts, no tokens.

## Screenshots
- Share to Discord via webhook as a second target next to Picsur.
- Burst / timer in the gesture settings, if the fork's detector exposes them.

## Housekeeping
- Move the remaining tunable constants into an "advanced" JSON section.
- Retire monado-frame; update the LinuxWiki page for the new setup.
- In-headset "controls" card listing every gesture (grip, trigger, A, B, stick,
  double-B, B-while-gripping).
